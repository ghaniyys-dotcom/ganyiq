# Database (inherited from PM2 env, but needed for Next.js loading)
DATABASE_URL=postgresql://ganyiq:***@localhost:5432/ganyiq

# LLM API Key — used by analyzer.ts for chat completions via 9router proxy
# Any value works since 9router (port 20128) does not require auth
OPENCODE_GO_API_KEY=ganyiq-9router-proxy

# Rate Limiting
RATE_LIMIT_PER_DAY=100

# Application URL
NEXT_PUBLIC_APP_URL=https://ganyiq.ganys.me
